# 🤖 AutoGen Team Builder

Build autonomous AI agent teams with a single command. Describe a task in natural language — **AutoGen AgentBuilder** will create a specialized team of AI agents that collaborate, search the web, and execute code to complete it.

![Streamlit](https://img.shields.io/badge/Streamlit-FF4B4B?logo=streamlit&logoColor=white)
![AutoGen](https://img.shields.io/badge/AutoGen-0078D4?logo=microsoft&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-2496ED?logo=docker&logoColor=white)

---

## ⚡ Quick Install

> **Prerequisite:** [Docker Desktop](https://docs.docker.com/get-docker/) must be installed and running.

```bash
git clone https://github.com/Jailtonfonseca/multi-agent-orchestrator.git
cd multi-agent-orchestrator
bash install.sh
```

The installer will:
1. ✅ Check Docker is installed and running
2. 🔍 Find an available port (default: `8501`)
3. 🐳 Build and start the container
4. 🏥 Wait for Streamlit to be ready
5. 🎉 Print the access URL

Then open **http://localhost:8501** (or the port shown by the installer).

---

## 🗑️ Uninstall

```bash
bash uninstall.sh
```

Stops the container, removes config files, and optionally deletes the Docker image.

---

## 📖 Usage

1. **Enter your API key** in the sidebar
2. **Choose a provider** — OpenRouter, OpenAI, Groq, or DeepSeek
3. **Select a model** or type a custom model ID
4. **Describe your task** in the text area
5. Click **🚀 Build Team & Execute**

### Supported Providers

| Provider | Example Models |
|---|---|
| OpenRouter | `openai/gpt-4o`, `anthropic/claude-3.5-sonnet` |
| OpenAI | `gpt-4-turbo`, `gpt-4o` |
| Groq | `llama3-70b-8192`, `mixtral-8x7b-32768` |
| DeepSeek | `deepseek-chat`, `deepseek-coder` |

### Built-in Tools

| Tool | Description |
|---|---|
| 🌐 Web Search | Real-time web search via DuckDuckGo |
| 💰 Crypto Price | Live cryptocurrency prices from CoinGecko |
| 🐍 Code Execution | Python code execution in the container |

---

## 🔧 Manual Install (without installer)

```bash
# Clone
git clone https://github.com/Jailtonfonseca/multi-agent-orchestrator.git
cd multi-agent-orchestrator

# Build and run
docker-compose up -d --build

# Open
open http://localhost:8501
```

Or run locally without Docker:

```bash
pip install -r requirements.txt
streamlit run app.py
```

---

## 🏗️ Architecture

```
Browser  ──►  Streamlit (port 8501)
                  │
                  ├──►  AutoGen AgentBuilder  →  Agent Team
                  │         └── GroupChat / Two-Agent Chat
                  │
                  ├──►  DuckDuckGo Search API
                  └──►  CoinGecko Price API
```

Single container — no database, no Redis, no backend. Everything runs within the Streamlit process.

---

## ⚙️ Configuration

| Variable | Default | Description |
|---|---|---|
| `APP_PORT` | `8501` | Port to expose Streamlit |

Edit `.env` (generated by `install.sh`) or set environment variables manually.

---

## 🛡️ Security

> [!NOTE]
> API keys are entered per-session in the sidebar and never stored on disk.

> [!WARNING]
> AutoGen executes Python code inside the container without sandboxing.
> For production, use Docker-in-Docker isolation.

---

## 📋 Useful Commands

```bash
# View logs
docker-compose logs -f app

# Restart
docker-compose restart

# Stop
docker-compose down

# Rebuild after editing app.py
docker-compose up -d --build
```
